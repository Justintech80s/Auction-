# Server-side eBay OAuth credentials used to mint short-lived application tokens
# for the official Browse API. Never expose these values in the browser extension.
EBAY_CLIENT_ID=
EBAY_CLIENT_SECRET=
