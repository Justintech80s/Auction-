{
  "name": "auction-valuation-core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "build:extension": "node scripts/build-extension.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
