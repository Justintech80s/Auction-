import test from 'node:test';import assert from 'node:assert/strict';import {createSerpApiShoppingProvider} from '../src/product-search/providers/serpapi-shopping.js';
test('shopping provider normalizes live store offers',async()=>{let requested;const fetchImpl=async u=>{requested=String(u);return {ok:true,json:async()=>({shopping_results:[{product_id:'p1',title:'Apple AirPods Pro 2',source:'Example Store',extracted_price:199.99,product_link:'https://store.example/item',thumbnail:'https://img.example/a.jpg'}]})}};const p=createSerpApiShoppingProvider({apiKey:'secret',fetchImpl});const offers=await p.search({brand:'Apple',model:'AirPods Pro 2',title:'AirPods Pro'});assert.equal(offers.length,1);assert.equal(offers[0].store,'Example Store');assert.equal(offers[0].itemPrice,199.99);assert.match(requested,/engine=google_shopping/);assert.match(requested,/api_key=secret/);});
test('shopping provider rejects missing credential',()=>assert.throws(()=>createSerpApiShoppingProvider({}),/credentials/));