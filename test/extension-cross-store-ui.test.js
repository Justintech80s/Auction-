import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';const html=fs.readFileSync(new URL('../extension/sidepanel/index.html',import.meta.url),'utf8');test('side panel exposes unified online price comparison region',()=>{assert.match(html,/Price Comparison/i);assert.match(html,/shared-comparison-list/);assert.match(html,/Lowest Price Found/i);assert.match(html,/Visit Store/i);});