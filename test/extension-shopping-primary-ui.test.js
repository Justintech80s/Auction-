import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';const html=fs.readFileSync(new URL('../extension/sidepanel/index.html',import.meta.url),'utf8');test('photo shopping results are the primary sidebar experience',()=>{assert.match(html,/Product Identified/);assert.match(html,/Lowest Price Found/);assert.match(html,/Price Comparison/);assert.match(html,/Savings Tips/);assert.doesNotMatch(html,/Resale Analysis|Deal Costs|Saved Presets/);});
