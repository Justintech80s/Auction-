import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';const read=p=>fs.readFileSync(new URL(`../${p}`,import.meta.url),'utf8');test('extension keeps HTTPS access for store links and backend access',()=>{const m=JSON.parse(read('extension/manifest.json'));assert.ok(m.host_permissions.includes('https://*/*'));});test('shopping sidebar is photo-search-first',()=>{const h=read('extension/sidepanel/index.html');assert.match(h,/Find Cheapest Price/i);assert.match(h,/Lowest Price Found/i);assert.match(h,/Price Comparison/i);assert.match(h,/Visit Store/i);assert.doesNotMatch(h,/Saved Presets|Marketplace fee|Payment processing|Holding cost/i);});test('photo path includes image evidence for product identification',()=>{const s=read('extension/sidepanel/shared-session.js');assert.match(s,/readAsDataURL/);assert.match(s,/image:\{dataUrl:data/);});
